[pytest]
markers =
    unit: fast, isolated, no I/O beyond in-memory data
    integration: exercises multiple modules together against real project data
    regression: locks in a previously-verified result (Module 1's deliberate design choices)
